# Helper Configuration for acme
driftfile /var/lib/chrony/drift
makestep 1.0 3

# Sync with the public-facing IPA host
{% for host in groups['ipa'] %}
server {{ hostvars[host]['ansible_host'] }} iburst
{% endfor %}

# Allow the private provisioning zone
allow {{ helper_zone_cidr }}
